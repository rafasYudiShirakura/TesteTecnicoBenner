<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:WpfApp.ViewModels"
        xmlns:v="clr-namespace:WpfApp.Views"
        mc:Ignorable="d"
        Title="Sistema de Gestão de Pedidos" Height="600" Width="1000"
        Background="{StaticResource WindowBackgroundBrush}">
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:PessoaViewModel}">
            <v:PessoaView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ProdutoViewModel}">
            <v:ProdutoView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:PedidoViewModel}">
            <v:PedidoView/>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5" Background="{StaticResource HeaderBackgroundBrush}">
            <Button Content="Pessoas" Command="{Binding NavigateCommand}" CommandParameter="Pessoa" Margin="5"/>
            <Button Content="Produtos" Command="{Binding NavigateCommand}" CommandParameter="Produto" Margin="5"/>
            <Button Content="Pedidos" Command="{Binding NavigateCommand}" CommandParameter="Pedido" Margin="5"/>
        </StackPanel>

        <ContentControl Grid.Row="1" Content="{Binding CurrentViewModel}" Margin="10"/>
    </Grid>
</Window>