<UserControl x:Class="WpfApp.Views.ProdutoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <DockPanel Grid.Column="0" Margin="0,0,10,0">

            <GroupBox Header="Filtros" DockPanel.Dock="Top">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Nome:"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding FiltroNome, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,10"/>

                    <TextBlock Grid.Row="0" Grid.Column="2" Text="Código:"/>
                    <TextBox Grid.Row="0" Grid.Column="3" Text="{Binding FiltroCodigo, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Valor Inicial:"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding FiltroValorInicial, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0"/>

                    <TextBlock Grid.Row="1" Grid.Column="2" Text="Valor Final:"/>
                    <TextBox Grid.Row="1" Grid.Column="3" Text="{Binding FiltroValorFinal, UpdateSourceTrigger=PropertyChanged}" Margin="0"/>
                </Grid>
            </GroupBox>

            <GroupBox Header="Registros">
                <DataGrid ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem}" AutoGenerateColumns="True"/>
            </GroupBox>
        </DockPanel>

        <GroupBox Grid.Column="1" Header="Cadastro/Edição">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Nome:"/>
                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding CurrentItem.Nome}"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Código:"/>
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding CurrentItem.Codigo}"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Valor:"/>
                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding CurrentItem.Valor}"/>

                <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,10,0,0">
                    <Button Content="Novo" Command="{Binding NewCommand}" Margin="0"/>
                    <Button Content="Salvar" Command="{Binding SaveCommand}"/>
                    <Button Content="Excluir" Command="{Binding DeleteCommand}" Style="{StaticResource DangerButtonStyle}"/>
                </StackPanel>
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>